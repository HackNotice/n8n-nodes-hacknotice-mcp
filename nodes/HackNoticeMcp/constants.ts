/**
 * Fixed HackNotice MCP Streamable HTTP URL (mcp-server `/mcp` path).
 * Not exposed in the credential UI — all HackNotice MCP traffic uses this host.
 */
export const HACKNOTICE_MCP_ENDPOINT_URL = 'https://mcp.hacknotice.com:13330/mcp';
